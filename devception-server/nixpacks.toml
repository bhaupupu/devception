# Install python3 and g++ via apt on the Ubuntu base image so we don't
# conflict with the Nixpacks Node provider (which pins nodejs_18 and would
# collide with a nixPkgs-supplied nodejs_20 in the same Nix env).
[phases.setup]
aptPkgs = ["python3", "g++"]
